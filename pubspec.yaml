name: financial_management
description: Using This app users will be able manage their costs.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  injectable: ^1.5.0
  flutter_bloc: ^7.3.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^8.1.0
  build_runner: ^2.1.4
  flutter_lints: ^1.0.4
  injectable_generator:
  mockito: ^5.0.16

flutter:
  uses-material-design: true